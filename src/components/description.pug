mixin description(mainUrl, text)
    if mainUrl && text
        // Описание | начало
        table(width="100%" border="0" cellpadding="0" cellspacing="0")
            tr
                td
                    div(style="font-size: 15px; font-weight: normal; line-height: 1.47; -webkit-text-size-adjust: none;")
                        a(target="_blank" href=`${mainUrl}` style="display: block; cursor: pointer; color: #333333; text-decoration: none;")= text
        // Описание | конец