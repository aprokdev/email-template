mixin header(text)
    if text
        // Заголовок | начало
        tr
            td(style="padding-top: 0; padding-bottom: 20px; text-align: left;")
                div(style="font-size: 16px; line-height: 1.5; color: #333333; font-weight: bold;  -webkit-text-size-adjust: none;")= text
        // Заголовок | конец
